{
  "compilerOptions": {
    "target": "ESNext",                          // Configura o alvo para a versão mais recente do JavaScript.
    "module": "ESNext",                          // Utiliza o módulo ESNext (recomendado pelo Vite).
    "moduleResolution": "Node",                  // Define a resolução de módulos para o estilo Node.js.
    "jsx": "react-jsx",                          // Habilita a transformação JSX no estilo do React 17+.
    "strict": true,                              // Ativa a verificação estrita para maior segurança no código.
    "esModuleInterop": true,                     // Permite a importação de módulos CommonJS com sintaxe ESModules.
    "skipLibCheck": true,                        // Ignora a verificação de tipos de arquivos de declaração.
    "forceConsistentCasingInFileNames": true,    // Força consistência de maiúsculas e minúsculas nos nomes dos arquivos.
    "allowSyntheticDefaultImports": true,        // Permite importar módulos sem a necessidade de um `default` no export.
    "resolveJsonModule": true,                   // Permite a importação de arquivos JSON como módulos.
    "composite": true                            // Necessário para compilar projetos referenciados em TypeScript.
  },
  "include": [
    "src/**/*",                                  // Incluir todos os arquivos TypeScript dentro da pasta src.
    "vite.config.ts"                             // Inclui a configuração do Vite.
  ],
  "exclude": [
    "node_modules",                              // Exclui a pasta node_modules.
    "dist",                                      // Exclui a pasta de build (dist).
    "build"                                      // Exclui a pasta de build (build).
  ]
}
